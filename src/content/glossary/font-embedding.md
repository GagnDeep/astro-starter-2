---
title: Font Embedding
description: Including font files within a document to ensure consistent rendering.
---

# Font Embedding

Font embedding is the practice of including the actual font files (or a subset of the characters used) within the document itself. This ensures that the document looks exactly the same regardless of whether the viewer has those specific fonts installed on their system.
