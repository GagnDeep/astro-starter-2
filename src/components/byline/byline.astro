---
interface Props {
  author: string;
  role: string;
  date: string;
}

const { author, role, date } = Astro.props;
---

<div
  class="flex items-center gap-4 py-4 border-t-2 border-neutral-900 dark:border-neutral-700 my-8 reveal-on-scroll"
>
  <div class="flex flex-col">
    <p class="font-display font-semibold text-neutral-900 dark:text-neutral-50 text-lg">
      <a href={`/authors/${author.toLowerCase().replace(/[^a-z0-9]+/g, "-")}/`} class="hover:text-orange-500 transition-colors">
        <span data-editable="text" data-prop="author">{author}</span>
      </a>
    </p>
    <p class="text-neutral-600 dark:text-neutral-400 text-sm">
      {role && <span data-editable="text" data-prop="role">{role}</span>}
      {role && <span class="mx-2">&middot;</span>}
      <span data-editable="text" data-prop="date">Last updated: {date}</span>
    </p>
  </div>
</div>
