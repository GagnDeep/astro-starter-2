---
import type { ImageMetadata } from "astro";
import { Picture } from "astro:assets";
import Markdown from "../../utility/markdown.astro";

interface Props {
  heading: string;
  subheading: string;
  image: {
    image_path: string;
    alt_text: string;
  };
  button?: {
    text: string;
    link: string;
  };
}

const { heading, subheading, image, button } = Astro.props;

const allImages = import.meta.glob<{ default: ImageMetadata }>(
  "/src/assets/**/*.{jpeg,jpg,png,gif,svg,webp,avif}",
  { eager: true },
);
const imageSrc = allImages[image.image_path]?.default ?? image.image_path;
---

<section class="py-16 md:py-24">
  <div
    class="mx-auto max-w-6xl px-8 flex flex-col md:flex-row items-center gap-12">
    <div class="flex-1 flex flex-col gap-6">
      <h1
        data-editable="text"
        data-prop="heading"
        class="text-4xl md:text-5xl font-bold leading-tight text-balance">
        {heading}
      </h1>
      <div class="text-xl text-[var(--text-color)] opacity-90 max-w-prose text-pretty">
        <Markdown content={subheading} dataProp="subheading" dataType="block" />
      </div>
      {
        button?.link && (
          <a
            href={button.link}
            class="mt-2 inline-block w-fit bg-[var(--color-brand-900)] text-[var(--color-support-100)] px-6 py-3 font-bold hover:bg-[var(--color-brand-500)] hover-transition">
            <span data-editable="text" data-prop="button.text">
              {button.text}
            </span>
          </a>
        )
      }
    </div>
    {
      image.image_path && image.alt_text && (
        <div class="md:ml-auto w-full max-w-md aspect-square bg-[var(--color-neutral-200)] shadow-md overflow-hidden relative">
          <Picture
            data-editable="image"
            data-prop-src="image.image_path"
            data-prop-alt="image.alt_text"
            alt={image.alt_text}
            src={imageSrc}
            class="absolute inset-0 w-full h-full object-cover"
            loading="eager"
            widths={[600, 800, 1200]}
            sizes="(max-width: 1200px) 100vw, 600px"
            formats={["avif", "webp"]}
          />
        </div>
      )
    }
  </div>
</section>
