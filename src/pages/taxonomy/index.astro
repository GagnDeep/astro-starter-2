---
import Layout from "../../layouts/Layout.astro";
import { getCollection } from "astro:content";

const pages = await getCollection("taxonomy");
---

<Layout title="Solar">
  <div class="max-w-4xl mx-auto px-4 py-16 prose">
    <h1>Regional Taxonomy</h1>
    <ul>
      {pages.map((page) => (
        <li>
          <a href={`/taxonomy/${page.id}`}>{page.data.title}</a>
        </li>
      ))}
    </ul>
  </div>
</Layout>
